% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getInference.R
\name{gamma_pval}
\alias{gamma_pval}
\title{Elementwise p-values for gamma with multiple-testing adjustment}
\usage{
gamma_pval(fit, method = "bonferroni", alternative = "two.sided")
}
\arguments{
\item{fit}{\code{rstan::stanfit} that contains parameter \code{gamma}.}

\item{method}{Adjustment method for \code{stats::p.adjust()} (default \code{"bonferroni"}).}

\item{alternative}{One of \code{"two.sided"}, \code{"less"}, \code{"greater"}.}
}
\value{
A list with:
\describe{
  \item{\code{pvalue}}{Matrix \code{m x r} of raw p-values.}
  \item{\code{adjpvalue}}{Matrix \code{m x r} of adjusted p-values.}
}
}
\description{
Computes posterior \emph{z}-scores for \eqn{\gamma} and converts them to
(one- or two-sided) normal-approximation p-values, then applies
\code{p.adjust(method)} across all elements.
}
\examples{
\dontrun{
  gp <- gamma_pval(fit, method = "BH", alternative = "two.sided")
  which(gp$adjpvalue < 0.05, arr.ind = TRUE)
}

}
