% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getInference.R
\name{getChisq_QSS}
\alias{getChisq_QSS}
\title{Chi-squared test on QSS statistics}
\usage{
getChisq_QSS(
  qss,
  w = 0,
  use_differencing = FALSE,
  df_method = "reduced",
  p_method = "chisq",
  n_bootstrap = 1000,
  alpha = 0.05,
  adjust_method = "holm",
  signal_position = c("first", "last", "middle", "max_deviation"),
  y = NULL,
  taus = NULL
)
}
\arguments{
\item{qss}{3D array [iterations, 4, time] from getQSS()}

\item{w}{Warm-up period}

\item{use_differencing}{If TRUE, use first-order differencing}

\item{df_method}{Method for degrees of freedom}

\item{p_method}{Method for p-values}

\item{n_bootstrap}{Bootstrap replicates}

\item{alpha}{Significance level}

\item{adjust_method}{P-value adjustment method}

\item{signal_position}{Method to determine signal position within a run of consecutive signals}

\item{y}{Original data (required only for signal_position = "max_deviation")}

\item{taus}{Quantile levels (required only for signal_position = "max_deviation")}
}
\value{
List with chi-squared statistics and diagnostic info
}
\description{
Chi-squared test on QSS statistics
}
\examples{
\donttest{
set.seed(123)
n <- 100
y <- rnorm(n)
taus <- c(0.05, 0.25, 0.5, 0.75, 0.95)
H <- getIsolatedShift(n, l = 20, w = 20)
fit <- getModel(y, taus, H = H, w = 20, fit_method = "map",
                map_hessian = FALSE, map_iter = 500)
eta <- getEta(fit, H = H)
qss <- getQSS(eta, taus = taus)
result <- getChisq_QSS(qss, w = 20)
}
}
