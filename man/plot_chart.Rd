% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getInference.R
\name{plot_chart}
\alias{plot_chart}
\title{Quick ribbon-style chart of y and posterior quantiles}
\usage{
plot_chart(
  y,
  q1,
  q25,
  q50,
  q75,
  q99,
  pval_chisq_adj,
  differences = 0,
  w = 0,
  FAP0 = 0.05
)
}
\arguments{
\item{y}{Numeric vector of length \eqn{n}.}

\item{q1, q25, q50, q75, q99}{3D arrays or matrices whose columns (or the 3rd
dimension) correspond to time; the function uses columnwise medians.}

\item{pval_chisq_adj}{Numeric vector of adjusted p-values (typically
\code{getChisq(...)$adjpvalue}).}

\item{differences}{Integer used only to shift the vertical lines so that
change points align with the differenced index.}

\item{FAP0}{Significance threshold (false-alarm probability), default \code{0.05}.}
}
\value{
Invisibly returns \code{NULL}; called for its plotting side-effect.
}
\description{
Plots the observed series \code{y} together with median trajectories of
posterior quantiles (\code{q1, q25, q50, q75, q99}), and marks time indices
with significant adjusted p-values from \code{\link{getChisq}}.
}
\details{
Vertical dashed red lines are drawn at indices where
\code{pval_chisq_adj <= FAP0}, shifted by \code{differences}.
}
\examples{
\dontrun{
  plot_chart(y, q1, q25, q50, q75, q99, inf$adjpvalue, differences = 1)
}

}
